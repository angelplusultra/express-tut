<%if(user){ %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">To-Deux</a>
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarColor01"
            aria-controls="navbarColor01"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
              <li class="nav-item">
                <a class="nav-link" href="/api">Home</a>
              </li>
            </ul>
            <ul class="navbar-nav">
                <a class="nav-item nav-link" href="#todos">Todos</a>
              <form method="post" action="">
                <label for="fileField"><img width="40"
                  class="rounded-circle nav-item profilePic border border-muted  border-2"
                  src="<%= !user.profilePic ? `nopic.jpg` : user.profilePic%>"
                  alt=""></label>
                <input hidden type="file" name="image" id="fileField">
            </form>
            
              <a class="nav-item nav-link" href="/auth/logout"><i class="fa fa-sign-out fa-lg" aria-hidden="true"></i></a>
            </ul>
          </div>
        </div>
      </nav>

<%} else{%>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">To-Deux</a>
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarColor01"
            aria-controls="navbarColor01"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
              <li class="nav-item">
                <a class="nav-link" href="/register">Register</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/login">Login</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
<%}%>


